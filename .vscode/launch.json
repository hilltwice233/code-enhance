{
  "configurations": [
    {
      // This launch script is only for color theme development.
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "name": "Color theme source",
      "request": "launch",
      "type": "extensionHost"
    },
    {
      // This launch script is only for vscode script development.
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/extension"],
      "name": "Extension preview",
      "request": "launch",
      "type": "extensionHost",
      "preLaunchTask": "npm:build"
    }
  ]
}
